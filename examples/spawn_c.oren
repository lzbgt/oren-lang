var done = 0

fn worker() {
    done = 1
    return nil
}

print("spawn_c: start")
var t = spawn worker()
oren_join(t)
if done == 1 {
    print("spawn_c: OK")
    exit(0)
}
print("spawn_c: FAIL")
exit(1)
