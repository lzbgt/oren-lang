# UI ideas (redirect; rolling)

This repo previously referenced a scratch file named `ui-idea.md` in discussions.

To avoid stale pointers and duplication, the current source of truth lives in `docs/`:

- `docs/GUI.md` — overall GUI/UI architecture for Oren (retained‑mode UI core + thin platform shims)
- `docs/GUI_PLATFORM_SHIMS.md` — actionable engineering plan for the Tier‑1 shim ABI + bring-up gates
- `docs/GUI_IMGUI_SHELL.md` — optional Dear ImGui overlay/shell role (devtools + bring-up accelerator)

If you are looking for “what should we build next?”, start with `docs/TODOS.md` (GUI section).
