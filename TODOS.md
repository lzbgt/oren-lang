# TODOs

This file is a concise pointer for active reweighted work. Detailed historical status remains in `docs/STATUS.md`, and focused evidence notes live under `project-doc/`.

## P1 / W4 - AVM iOS Production Readiness

- Keep the new `make verify-libavm-ios` gate green. It now builds `LibAVM.xcframework`, exports the embedder headers, compiles a tiny Oren source to `.obc`, links that bytecode into iOS device/simulator smoke binaries, and runs the same `.obc` through the host embed API with argv plus VirtualFS input/output, VirtualNET fixtures, VirtualPROC fixtures/default exits, and captured stdout retrieval/clear.
- Keep `make verify-compiler-in-avm-ios-chain` green. It now builds `stdlib_bundle.obc` and `oren.obc`, runs the compiler `.obc` inside AVM with VirtualFS stdlib resources, extracts `out.obc`, and runs the compiled output.
- Continue the iOS host-adapter SDK (`OrenAVMKit`) after the retained TIME/NET/GFX/FS slices. Current SDK package builds `OrenAVMKit.xcframework`, exposes deterministic and interactive app defaults, runs OBC on virtual FS/NET/PROC with stdout capture, verifies wall-clock `time.sleep_ms`, verifies app file/directory mounts into VirtualFS and VFS export back to host files, verifies allowlisted `URLSession` prefetch into VirtualNET and live allowlisted host fetch during `std:net/avm.try_get_text(url)`, verifies binary GFX frame retrieval plus host-injected binary pointer/resize/key/text events through the GFX mailbox, exposes decoded Oren-side `std:ui/avm.next_event()` maps, and ships a default UIKit/CoreGraphics `OrenAVMGraphicsView` for the current `fill_rect`/`text`/`stroke_line`/`circle` subset. The `OGF0` frame header now carries frame sequence, native drawable size, and target refresh hint metadata; AVM validates `OGF0` frames and `OGE0` host input events before accepting them, with curated gates for malformed frames, op-count cap rejection, frame I/O-budget rejection, input queue depth, non-1000 resize scale propagation, latest-frame replacement/clear semantics, and FIFO pointer ordering. Next: compiler helper package, package store helper, and the game-grade GUI path: display-link pacing gates, retained resource handles, Metal/`MTKView`, richer 2D/3D rendering, and richer text input/IME helpers. Design notes: `project-doc/ios_avm_sdk_design_20260531.md`, `project-doc/avm_ui_render_performance_design_20260531.md`.
- Treat virtual resources as app-facing host adapters, not test-only fixtures. The iOS SDK default should make safe real work convenient: TIME has deterministic and wall-clock modes; FS has explicit app file/directory mount and export helpers; NET has allowlisted `URLSession` prefetch plus an allowlisted live host-fetch callback for OBC `std:net/avm`; PROC should remain virtual/app-command based on iOS rather than arbitrary subprocess; UI has binary mailboxes plus UIKit fallback and needs the Metal/game-grade path. Each default must remain capability-declared and budgeted before OBC runs.
- Extend AVM NET beyond HTTP-style body fetch to the rest of the app-facing network surface. TCP/UDP should not expose raw iOS sockets to OBC; design a capability-gated session-handle protocol covering connect/listen/send/recv/close, DNS policy, async readiness, cancellation, byte budgets, and iOS lifecycle behavior, then map `std:net/tcp` / `std:net/udp` facades onto that protocol.
- Add a Swift/Objective-C iOS smoke host that opens `AvmEmbedHandle`, loads bundled `.obc` resources, feeds source/stdlib files through `avm_embed_vfs_put`, extracts `out.obc` through `avm_embed_vfs_get`, injects optional deterministic network/process fixtures with `avm_embed_vnet_put` and `avm_embed_vproc_put`, runs with virtual FS/PROC/NET, and reports `AvmEmbedResult` plus captured stdout into the Note UI.
- Continue the AVM graphics bridge for iOS: binary frame and input mailboxes are implemented for current `std:ui` v0 frames and pointer/resize/key/text events; the first UIKit/CoreGraphics renderer adapter is implemented for `fill_rect`/`text`/`stroke_line`/`circle`; protocol gates now cover frame validation, op-count caps, frame I/O budget, input queue depth, latest-frame replacement/clear, non-1000 resize scale, and FIFO pointer ordering. Next make the protocol game-grade before feature expansion: display-link pacing, retained resource handles, Metal/`MTKView` rendering, and richer command/resource protocols without exposing platform objects to Oren code. Design notes: `project-doc/avm_ios_graphics_design_20260529.md`, `project-doc/avm_ui_render_performance_design_20260531.md`.
- After the GUI bridge release gate, create a curated signed public OBC store repo so the iOS app can download, verify, and run OBC app experiences. Design note: `project-doc/obc_store_distribution_design_20260529.md`.
- Add an AVM fixture manifest runner: fixture path, expected return code/error, required env budgets, backend policy, deterministic mode, host-effect expectations, and release-gate inclusion.
- Keep the AVM stdlib OBC surface gate green. It rebuilds `stdlib_bundle.obc`, compiles a representative app with `--stdlib-mode obc`, and proves app-facing exports from `std:buffer`, `std:bytes`, `std:cbor`, `std:encoding/base64`, `std:crypto/pem`, `std:crypto/sha1`, `std:crypto/sha256`, `std:crypto/x509`, `std:json`, `std:linalg`, `std:math`, `std:net/avm`, `std:regex`, `std:strings`, `std:time`, `std:ui/avm`, and `std:yaml` link and run in AVM, including `STD_linalg_dot_f64`.
- Finish AVM/OBC stdlib feature completeness with a manifest-driven exported-symbol link gate for every app-facing std module and explicit host-only exclusions. Next candidates: deeper CBOR/YAML/regex/PEM/X509 coverage, portable crypto expansion, app-sandbox FS facade coverage, and live/asynchronous NET policy if OBC apps need it.
- Expand compiler-in-AVM packaging from the current smoke gate to app-bundle resource loading and broader compiler/stdlib surface coverage.
- Finish embedder lifecycle hardening: make allocator ownership reentrant or explicitly single-VM guarded, define app-level failure policy for teardown leaks, and keep host-only subprocess paths unavailable in iOS builds.

Evidence and verdict: `project-doc/ios_avm_readiness_20260507.md`.

## P1 / W4 - Scientific Math Stdlib

- Keep `std:math` host-libm-free and bytecode/AVM-gated for app embedding. `pow` / `power` now cover integer, negative, and fractional positive-base exponents including `power(2,-1)` and `power(2,4.3)`; common C/C++-style additions now include `fabs`, `fmin`, `fmax`, `trunc`, `fmod`, `log`, `log10`, `exp10`, and `hypot`.
- Continue expanding toward C/C++ mathlib breadth with focused native and AVM fixtures for each function family before exposing it as app-stable; do not expose `tan`/`asin`/`acos` until their deterministic implementations pass both backends.

## P3 / W2 - Language Ergonomics

- Add anonymous imports after higher-priority AVM app surfaces are gated. Proposed syntax: `import . "std:math"` to bring public module entities into the current namespace; optionally consider `use "std:math"` only if it does not conflict with existing grammar or dependency scanning. The implementation must define collision rules, visibility behavior, cache/import-scan support, and cross-backend fixtures before retention.
