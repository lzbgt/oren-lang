import list "std:list"

fn main() {
    var n = 2000000
    var a = []
    var b = []
    var i = 0
    while i < n {
        list.push(a, (i * 3 + 1) % 1000)
        list.push(b, (i * 7 + 2) % 1000)
        i = i + 1
    }

    var sum = 0
    i = 0
    while i < n {
        sum = sum + a[i] * b[i]
        i = i + 1
    }

    print(sum)
    return 0
}
