fn main() {
    var i = 0
    var sum = 0
    var xs = oren_new_list(0)
    while i < 4 {
        var v = oren_list_len(xs)
        sum = sum + v
        xs = oren_new_list(0)
        oren_list_push(xs, i)
        i = i + 1
    }
    if sum < 0 {
        exit(41)
    }
}
