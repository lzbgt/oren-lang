// Runtime diagnostic fixture:
// - exits non-zero
// - prints a machine-readable OREN_DIAG header
// Expected failure diagnostic fixture (used when manually testing `OREN_DIAG` output).

oren_fail(42, "diagnostic fixture")
