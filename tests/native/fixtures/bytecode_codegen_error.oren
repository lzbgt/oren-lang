// Bytecode backend codegen diagnostic fixture.
//
// This fixture should:
// - fail compilation under --backend bytecode
// - emit OREN_DIAG kind=codegen code=1
//
// Rolling note: the bytecode backend has a deterministic constructor lowering that
// treats struct values as map-shaped records; mismatched constructor arity should be
// a codegen error (not a parse error) and must produce OREN_DIAG kind=codegen.

struct Node { value }

// Arity mismatch: Node expects 1 positional arg.
var n = Node(1, 2)
