// Bytecode backend codegen diagnostic fixture.
//
// This fixture should:
// - fail compilation under --backend bytecode
// - emit OREN_DIAG kind=codegen code=1
//
// Rolling note: spawn supports call-site spread, but varargs+spread in spawn is
// still rejected (needs dynamic varargs packing).

fn f(x, ...rest) { return x }

var xs = [1]
spawn f(1, xs...)
