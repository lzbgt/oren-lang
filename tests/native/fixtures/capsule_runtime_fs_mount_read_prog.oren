fn main() {
    // Runtime capsule FS test (mount read):
    // - Under capsule mode, FS mounts can map a virtual path prefix to a host directory.
    // - This program reads via the virtual prefix "v/".
    var b = oren_read_bytes("v/in.txt")
    print(oren_int_to_string(oren_list_len(b)))
}

