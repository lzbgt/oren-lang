// Test runner smoke: should be executed via `oren test` (no main required).

test "assert_smoke" {
    assert(true)
    assert(1 + 2 == 3, "math ok")
}

test "list_sum" {
    var xs = [1, 2, 3]
    var s = 0
    for x in xs { s = s + x }
    assert(s == 6, "sum ok")
}
