// Meta fixture: multi-format serde derive in schema.
//
// Legacy test-harness note (no dedicated runner today):
// This file used to be exercised by a separate harness to validate that `oren meta`
// exports normalized serde schema fields deterministically.

@serde(formats="json,yaml", tag="User")
struct User {
    @serde(rename="user_id")
    id: i32,
    name: string
}

fn main() {
    // no-op
    exit(0)
}
