@oren.package(runtime_profile="capsule", cap_allow_domains="", budget_gas=100000, budget_wall_ms=1000)
var native_package_policy_gas_native_fail = 1

fn main() {
    print("native package policy native fail")
    exit(7)
}
