fn blocked_nested_literal() {
    var f = || 7
    yield
    return f()
}

fn blocked_loop_yield() {
    while false {
        yield
    }
    return 0
}

fn main() {
    var keep = [blocked_nested_literal, blocked_loop_yield]
    return oren_list_len(keep) - 2
}
