import gen "std:generator"

fn inner_worker(co, args) {
    return args[0] + 1
}

fn outer_worker(co, args) {
    var inner = gen.start(inner_worker, args)
    if inner == nil { return 1 }
    return 0
}

fn main() { return 0 }
