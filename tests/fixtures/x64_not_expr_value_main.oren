// x86_64 native backend fixture (rolling bring-up):
// - exercises `!` in expression context (bool-as-int 0/1)

fn main() {
    var x = 0
    // !0 => 1
    // Keep a distinctive exit code so remote-run gates can disambiguate fixtures.
    return (!x) + 10 // 11
}
