fn before_ready() {
    return 0
}

fn after_ready() {
    return 0
}

fn ready_worker() {
    before_ready()
    yield
    after_ready()
    return 1
}

fn main() {
    var keep = [before_ready, after_ready, ready_worker]
    return oren_list_len(keep) - 3
}
