fn add7(a, b, c, d, e, f, g) {
    // Exercises stack args on both ABIs:
    // - Linux SysV: arg7 on the stack
    // - Windows x64: arg5+ on the stack
    return a + b + c + d + e + f + g
}

fn main() {
    return add7(1, 2, 3, 4, 5, 6, 7) // 28
}

